<%= form_for(@project_strong) do |f| %>
  <% if @project_strong.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@project_strong.errors.count, "error") %> prohibited this project_strong from being saved:</h2>

      <ul>
      <% @project_strong.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label "Nazwa projektu" %><br>
    <%= f.text_field :name, :class => "form-control" %>
  </div>
  <div class="field">
    <%= f.hidden_field :user_id %>
  </div>
  <div class="field">
    <%= f.label "Opis" %><br>
    <%= f.text_area :description %>
  </div>
  <div class="field hidden">
    <%= f.label "Opis" %><br>
    <%= f.select :project_types_id, ProjectTypes.all.collect { |p| [ p.name, p.id ] }, include_blank: false %>

  </div>
  <div class="actions">
    <%= f.submit "Utwórz", :class => "btn btn-primary" %>
  </div>
<% end %>
